# 和弦轉調工具 — 專案說明

一個單一 HTML 檔的網頁工具：讀取「圖片／PDF 的和弦譜」或「文字和弦譜」，自動辨識出和弦，讓使用者選擇升降 Key 後，把新和弦標註回原譜上輸出。

---

## 一、用什麼程式寫的？

整個專案就是 **一個 HTML 檔**（`和弦轉調工具.html`），裡面包含：

| 部分 | 技術 |
|---|---|
| 介面 | HTML + CSS（無任何框架） |
| 邏輯 | 原生 JavaScript（無框架、無建置工具） |
| 文字辨識 (OCR) | [Tesseract.js](https://tesseract.projectnaptha.com/) v5 — Google Tesseract OCR 引擎編譯成 WebAssembly 的版本 |
| PDF 讀取 | [pdf.js](https://mozilla.github.io/pdf.js/)（Mozilla，把 PDF 每頁畫成圖片） |
| PDF 輸出 | [jsPDF](https://github.com/parallax/jsPDF)（把標註好的頁面合成 PDF 下載） |
| 影像處理 | 瀏覽器內建 Canvas API（放大、灰階、遮罩、覆蓋繪字） |

三個函式庫都是從 CDN 載入，不需要安裝任何東西。

## 二、本地還是雲端？要運算嗎？

**全部在你的電腦本地運算，沒有伺服器。**

- 圖片、PDF、辨識過程、轉調結果 **完全不會上傳**，隱私安全。
- OCR 引擎（Tesseract）以 WebAssembly 形式直接在瀏覽器裡執行，吃的是你電腦的 CPU。
- 唯一需要網路的時刻：**第一次開啟時**從 CDN 下載函式庫和英文辨識模型（約 15 MB，之後瀏覽器會快取）。
- 運算量：文字轉調幾乎零成本（毫秒級）；圖片 OCR 每頁約 5～20 秒（本工具每頁會跑 2～3 輪辨識以提高辨識率，見下文），一般筆電即可，不需要顯卡。

## 三、整體架構

```
┌─────────────────────────────────────────────┐
│  和弦轉調工具.html                          │
│                                             │
│  ┌───────────────┐   ┌───────────────────┐  │
│  │ 分頁1：圖片譜 │   │ 分頁2：文字譜     │  │
│  │ 圖片/PDF→OCR  │   │ 貼上文字→即時轉調 │  │
│  │ →標註→下載    │   │ →複製/下載 .txt   │  │
│  └───────┬───────┘   └─────────┬─────────┘  │
│          └──────────┬──────────┘            │
│              和弦核心邏輯（共用）           │
│        解析 / 轉調 / 升降記號 / OCR誤讀修正 │
└─────────────────────────────────────────────┘
```

## 四、和弦核心邏輯（樂理部分）

### 1. 和弦解析
用正規表達式把一個字串拆成 `根音 + 屬性後綴 + 斜線低音`：

```
Am7  → 根音 A、後綴 m7
F#m7-5 → 根音 F#、後綴 m7-5
C/G  → 根音 C、低音 G
```

後綴只允許合法的和弦記號（maj、min、dim、aug、sus、add、數字、括號、#、b…），
所以「Hello」「Do」這種字會被判定為「不是和弦」。

### 2. 轉調
把音名對應到 0～11 的半音數（C=0、C#=1 … B=11），加上要移的半音數後 mod 12 再轉回音名。根音和斜線低音分別轉，後綴不變：

```
G/B 升 2 個半音 → A/C#
```

### 3. 升降記號的選擇（♯ 還是 ♭）
- 使用者可手動指定用 ♯ 或 ♭。
- 「自動」模式：優先看目標調性的慣例（F、Bb、Eb、Ab、Db、Gb 調用 ♭，其他用 ♯）；若只按半音升降沒指定目標調，就沿用原譜多數的寫法。

## 五、文字譜模式的作法

1. 逐行判斷是否為「和弦行」：把該行拆成字詞，若 60% 以上能解析成和弦（小節線 `|`、`x2`、`N.C.` 等中性符號不計分），就視為和弦行。
2. 和弦行逐字轉調，並**保持原本的欄位對齊**（和弦要對準歌詞的位置）。
3. 另支援 `[C]夾在歌詞[G]中` 的行內標記格式。
4. 結果即時顯示、和弦以紅色標亮，可複製或下載 .txt。

## 六、圖片／PDF 模式的作法（本專案最核心的部分）

OCR 對「和弦譜」這種特殊排版的辨識率天生很差，因此做了一整套處理管線：

### 步驟 1：載入
- 圖片直接載入；PDF 用 pdf.js 逐頁畫成高解析度圖片（多頁可切換）。

### 步驟 2：譜型自動偵測
掃描每一列像素的「墨水密度」與「最長**連續**墨水段」。若有 10 列以上同時滿足「密度超過 25%」且「連續段超過寬度一半」（真正橫貫整頁的線），就是**五線譜**；否則是**文字譜**（含簡譜）。兩者用不同流程。
只看密度不看連續性會把**簡譜**誤判成五線譜——簡譜的節拍底線是一段一段的短線，密度夠高但不連續；誤判後譜線遮罩會把較寬粗的和弦行整行塗白，造成該行完全辨識不到（實例：我唯一渴望 最後一行）。

### 步驟 3：前處理
- 放大：文字譜 3 倍、五線譜 5 倍（OCR 對小字的辨識率隨解析度大幅提升）。
- 五線譜額外做「**譜線遮罩**」：把墨水密度高的列整列塗白，並把譜線之間的小縫隙一併補滿（蓋掉音符）。這樣 OCR 看到的就只剩上方的和弦文字，不會被譜線干擾。

### 步驟 4：多輪辨識取聯集
同一頁跑 2～3 輪 OCR，互補漏抓：
1. 灰階＋提高對比版
2. 原色版（兩版的辨識結果常常不同，取聯集）
3. 五線譜再加一輪「稀疏模式」：只在已確認的和弦列高度帶內、不與現有結果重疊的位置撿漏。

重疊的偵測框合併時保留資訊較完整者（例如 `C/G` 優於 `C`）。

### 步驟 5：OCR 誤讀修正
和弦字常被 OCR 讀錯，累積實測建立了一套修正規則：

| OCR 讀到 | 實際 | 原因 |
|---|---|---|
| `Ic`、`lc`、`\|C` | C | 小節線黏字（僅文字譜啟用） |
| `lam`、`EC` | Am、C | 同上／反覆記號 `\|\|:` 黏字 |
| `FIC`、`GIF` | F/C、G/F | 斜線被讀成 I |
| `FG`、`GB` | F/G、G/B | 斜線消失（需通過「低音是否為合理轉位音」檢查，排除 FB 這種誤組） |
| `E?`、`Em'`、`Fmaj?` | E7、Em7、Fmaj7 | 上標的小字 ⁷ |
| `ETB`、`ETIG:` | E7/B、E7/G# | 上標 7 讀成 T、# 讀成 : |
| `om?` | Dm7 | D 讀成 o |
| `8` | Bb | ♭ 記號無法辨識 |
| `c`、`Cc`、`Co` | C | 大小寫／黏字 |
| `Gm/7`、`Dm/7` | Gm7、Dm7 | 上標小字 ⁷ 前多讀出斜線 |
| `_Bb/C` | Bb/C | 前導底線雜訊 |
| `Bom/F` | Bbm/F | ♭ 讀成 o |

### 步驟 6：雜訊過濾（避免把歌詞誤當和弦）
中文歌詞會被英文 OCR 讀成亂碼，偶爾亂碼恰好像個和弦（如單獨的「A」）。過濾規則：
- 逐行統計「和弦」與「可疑雜訊字」的數量，雜訊多於和弦的行不採信。
- 已確認 3 個以上和弦的「強和弦行」，同行低信心的字可零門檻救回。
- 邊緣案例用「字高比對」：和弦字高度應與其他和弦相近，歌詞誤判通常字比較高。
- 整行都是光桿單字母且 ≥6 個 → 多半是歌詞誤讀，整行拒絕。
- 五線譜的小節編號（14、18…）不會被誤修成和弦。

### 步驟 7：標註與輸出
- 對每個和弦，取其外框周圍的背景色，把原和弦**覆蓋**掉後用選定顏色寫上新和弦（也可選「標在上方」不覆蓋）。
- 介面上：藍框＝OCR 抓到的（點一下排除誤判）、綠框＝手動加的（點一下刪除；**可拖曳移動、拖右下角手把等比例縮放覆蓋範圍**）、點空白處＝手動補上漏抓的和弦。
- 控制列有「標註字級」滑桿（50%–300%）：**全頁統一字級**，一動就把所有標註（含已辨識到的藍框和弦與手動綠框）一起改成同一個字體大小。基準是該頁 OCR 框高的中位數，所以 100% ≈ 原譜和弦大小。字級調大時，覆蓋原和弦的白底範圍會跟著文字一起變大。
- 下載：本頁 PNG，或全部頁面合成一份 PDF。

## 七、辨識率實測

| 測試譜 | 類型 | 辨識率 |
|---|---|---|
| 在神沒有難成的事 | 文字和弦譜（含前奏小節線、靈歌反覆記號） | 46/46 |
| 無價至寶 p1 | 五線譜 | 34/34（另有 1 個 B♭ 附近的誤判框，點掉即可） |
| 無價至寶 p2 | 五線譜 | 19/19（反覆結尾下方和弦可自動救回；仍有 1 個既有音符誤框可點除） |

## 八、已知限制

- ♭ 記號 OCR 幾乎認不得（已用「8→Bb」規則部分補救）。
- 解析度太低、字太小、手寫譜的辨識率會明顯下降。
- 反覆記號、連結線等符號若讓 OCR 完全沒有輸出，仍可能需要手動補和弦；能形成可信和弦窄帶的反覆結尾已有專屬救援。
- OCR 模型第一次載入需要網路。

## 九、如果要進一步改進

- 換用專門的樂譜 OCR（如 Audiveris、oemer 等 OMR 引擎）可同時辨識音符，但就無法維持「單一 HTML、零安裝」的形態。
- 可加入「和弦進行合理性」檢查（例如依調性判斷 B/E 這種罕見和弦大概率是誤判）。
- 支援簡譜（1 4 5 級數記號）轉調。

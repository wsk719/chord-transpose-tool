---
task_id: "0b534434-5a75-4bdf-a9aa-8c0d2a97ce38"
thread_id: ""
status: completed
created_at: "2026-08-24T12:18:11+08:00"
updated_at: "2026-08-24T12:27:14+08:00"
workspace_root: "D:\\AI\\譜"
title: "修復 PDF 樂譜最後重複記號誤辨識與未顯示"
---

# 修復 PDF 樂譜最後重複記號誤辨識與未顯示

## 成功條件

- [x] 使用附件 PDF 可重現並定位最後一個反覆記號被辨識成 `A`／未顯示的最早異常。
- [x] 最後一個反覆記號在分析結果中保持為結構記號（不是和弦 `A`），且預覽畫面可見。
- [x] 新增能覆蓋此案例的自動化回歸測試，相關既有測試保持通過。
- [x] 實際重新分析附件或等價裁切輸入，核對資料輸出與畫面渲染結果。

## 範圍

- 檢查附件 PDF 的相關頁面、OCR 後處理、反覆記號分類及結果渲染資料流。
- 修改達成上述成功條件所需的最小程式碼與測試；必要時同步建置產物 `docs/index.html`。

## 不在範圍內

- 不重新設計整套 OCR 模型或與本案例無關的 UI。
- 不改寫附件 PDF，也不處理其他未證實的辨識問題。

## 關鍵假設與風險

- 假設附圖來自目前工作區的 `和弦轉調工具.html`／`docs/index.html`。
- OCR 可能受瀏覽器、PDF.js、Tesseract 版本及頁面縮放影響；優先以可重複的後處理單元測試固定行為。
- 工作樹已有其他計畫、技能與輸出目錄的未追蹤／刪除狀態，必須避免納入或覆寫。

## 執行計畫

- [x] 建立基線並確認成功條件。
- [x] 完成最小連貫變更或分析。
- [x] 執行驗證並核對結果。
- [x] 完成文件、記憶與 Git 收尾。

## 執行紀錄

### 2026-08-24T12:18:11+08:00

- 已建立持久化任務計畫。
- 基線以完整附件重現：第二列最右 `:||` 有藍框，升高 1 半音後被覆蓋為假 `A#`。
- 診斷證明原色 OCR 輪為 `I: / F / C/E / |Am / Ke / a`；少一個小節線使該輪退回一般解析，低信心 `a` 被救援為 A，之後與灰階正確結果聯集。
- 修正強反覆列採專用候選、不混回一般解析，並允許末尾小寫 `a` 在嚴格上下文中只補作結構證據。
- 已移除本機診斷輸出、重建 `docs/index.html`，並更新回歸測試與專案知識。

## 驗證結果

- Red：新增第二列 fixture 後 `node tests/test_ocr.js` 為 31 通過、1 失敗。
- Green：原始碼與 `docs/index.html` 各 32 項 OCR 測試通過；兩者各 73 項互動／手機測試通過。
- `git diff --check` 通過；搜尋確認正式原始碼與部署版沒有本次 `repeat-probe` 診斷殘留。
- 瀏覽器以完整附件重新分析：原調列尾無藍色和弦框；升高 1 半音後 `F / C/E / Am / G` 正常改為 `F# / C#/F / A#m / G#`，列尾仍為原 `:||`，沒有假 `A#`。

## 剩餘事項

- 無。已確認暫存區只包含本任務的原始碼、部署版、測試、知識與計畫；工作樹其他刪除及未追蹤內容均未納入。

## 模型接手指引

1. 先依 `task_id`／`thread_id` 辨識同一任務，再核對目前檔案與版本控制狀態。
2. 不把本紀錄視為唯一現況證據；重新執行與下一步相關的檢查。
3. 從第一個未勾選步驟接續，並在每個實質里程碑開始與完成時同步本檔。

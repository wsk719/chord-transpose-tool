---
task_id: "8d677b75-66d2-42cf-bc50-a344372005f8"
thread_id: ""
status: completed
created_at: "2026-08-24T10:46:00+08:00"
updated_at: "2026-08-24T10:55:33+08:00"
workspace_root: "D:\\AI\\譜"
title: "整合譜面更換與預覽區並移除內層捲軸"
---

# 整合譜面更換與預覽區並移除內層捲軸

## 成功條件

- [x] 載入圖片／PDF 後，「更換譜面」不再占用獨立橫列，而是位於實際譜面預覽區內。
- [x] 未載入檔案時，原本的點選、拖放、貼上與鍵盤選檔入口仍可使用。
- [x] 譜面預覽不再產生自己的垂直捲軸；長譜面由網頁唯一的垂直捲軸瀏覽。
- [x] 原始碼與部署版的既有互動測試、語法檢查及建置均通過，並完成桌機／手機版面檢視。

## 範圍

- 調整單頁 HTML 的上傳區載入後樣式、預覽容器捲動行為與相關前端測試。
- 重新產生 `docs/index.html`，保持部署版與原始碼一致。

## 不在範圍內

- 不改 OCR、轉調、標註資料或匯出行為。
- 不取消手動放大時必要的水平捲動能力。

## 關鍵假設與風險

- 使用者所稱下方「拖曳 BAR」是由 `#canvasWrap` 高度上限形成的內層垂直捲軸；整頁右側捲軸應保留。
- 預覽改由整頁垂直捲動後，觸控單指瀏覽仍須把垂直位移交給 `window`；水平放大瀏覽仍由預覽容器處理。
- 工作樹已有 `.agents/skills/` 刪除／新增及 `output/`、`tmp/` 未追蹤內容，本任務不得納入或覆寫。

## 執行計畫

- [x] 建立基線並確認成功條件。
- [x] 新增版面回歸斷言並確認會攔住舊行為。
- [x] 完成上傳入口與預覽整合、移除內層垂直捲動。
- [x] 執行驗證並核對結果。
- [x] 完成文件、記憶與 Git 收尾。

## 執行紀錄

### 2026-08-24T10:46:00+08:00

- 已建立持久化任務計畫。
- 基線確認 `#canvasWrap{overflow:auto;max-height:75vh}`（手機為 60svh）造成內層垂直捲軸；`drop.compact` 載入後仍是獨立的整列區塊。
- `git status` 顯示既有 `.agents/skills/`、`output/`、`tmp/` 變更，將與本次修改隔離。
- 先新增 5 項版面回歸斷言，舊版結果為 65 通過／5 失敗；實作後同檔共 73 項全數通過。
- 新增 `dockUploadToPreview()`：辨識完成後把既有上傳入口移入 `#canvasArea`，呈現右上角小型「更換譜面」按鈕，保留原有點選、拖放、貼上與鍵盤行為。
- `#canvasWrap` 改為水平 `auto`、垂直 `hidden` 且不限制高度；觸控 `panBy()` 的垂直位移與縮放錨點改交給整頁捲動。
- 瀏覽器實測桌機與 390×844 手機載入狀態：更換入口皆完整位於預覽區；桌機 wrap 893/893、手機 214/214，均無垂直 overflow，整頁保留垂直捲動。
- `python build_site.py` 成功重新產生 `docs/index.html`，vendor 10 個檔案共 17.7 MB 均存在。
- 原始碼與部署版各通過 73 項 jsdom 介面／觸控斷言、14 項 OCR 回歸斷言與所有 inline script 語法編譯；`git diff --check` 無錯誤。
- 已把新的預覽區結構與捲動契約補入 `project_knowledge.md`；未修改其他長期文件。

## 驗證結果

- Red：實作前 `tests/test_mobile.js` 為 65 通過／5 失敗，能攔住未整合入口、仍有限高與內層垂直捲動的舊行為。
- Green：`node tests/test_mobile.js` 與 `node tests/test_mobile.js docs/index.html` 各 73/73 通過。
- OCR：`node tests/test_ocr.js` 與 `node tests/test_ocr.js docs/index.html` 各 14/14 通過。
- 語法：Node `new Function` 編譯原始碼 1 段、部署版 2 段 inline script，均成功。
- 視覺：1440×900 與 390×844 實際載入測試譜面；按鈕位於 `#canvasArea` 內，預覽無垂直 overflow，頁面可垂直捲動。

## 剩餘事項

- 無；已完成可安全分離的提交前檢查，僅待緊接著建立本次 commit，不 push 或部署。

## 模型接手指引

1. 先依 `task_id`／`thread_id` 辨識同一任務，再核對目前檔案與版本控制狀態。
2. 不把本紀錄視為唯一現況證據；重新執行與下一步相關的檢查。
3. 從第一個未勾選步驟接續，並在每個實質里程碑開始與完成時同步本檔。

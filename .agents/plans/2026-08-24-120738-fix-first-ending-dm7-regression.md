---
task_id: "442aca77-1dab-48fe-b665-38781eae0b99"
thread_id: ""
status: completed
created_at: "2026-08-24T12:07:38+08:00"
updated_at: "2026-08-24T12:12:04+08:00"
workspace_root: "D:\\AI\\譜"
title: "修正窄帶裁切後第一結尾 Dm7 漏辨識"
---

# 修正窄帶裁切後第一結尾 Dm7 漏辨識

## 成功條件

- [x] 完整附件 PDF 第 2 頁最後一列第一結尾的 `Dm7` 恢復辨識。
- [x] 同頁第一列、下方有直線的 `C` 仍可辨識，第二結尾 `Dm7 / G7 / C` 不退化。
- [x] 原始版與部署版的 OCR／互動測試、建置與正式無快取端到端驗證通過。

## 範圍

- 修正縮短和弦窄帶後產生的反覆結尾 OCR 形態相容性、測試與部署產物。

## 不在範圍內

- 不撤銷上一版避免垂直小節線黏住 `C` 的窄帶裁切，也不修改 PDF。

## 關鍵假設與風險

- 完整原檔使用 `cy + 0.5h` 後，頁尾 PSM 7 實際輸出為 `pm? / G / [12.Dm7 / G7 / Cc`；`[12.Dm7` 未被既有反覆結尾正則接受，故積極窄帶解析未啟動。
- 擴充標記解析必須仍限於「數字 + 點／冒號／連字號 + 可解析和弦」形態，避免一般歌詞觸發。
- 工作樹既有 skill 刪除與未追蹤產物不屬本任務，不修改或提交。

## 執行計畫

- [x] 建立基線並確認成功條件。
- [x] 完成最小連貫變更或分析。
- [x] 執行驗證並核對結果。
- [x] 完成文件、記憶與 Git 收尾。

## 執行紀錄

### 2026-08-24T12:07:38+08:00

- 已建立持久化任務計畫。
- 已由使用者截圖確認回歸位置為第 2 頁最後一列第一結尾 `Dm7`；上一版端到端診斷已固定新窄帶原始輸出 `[12.Dm7`。
- 新增兩項回歸測試後取得 Red：原始版 26 項通過、2 項失敗，分別證明 `[12.Dm7` 無法剝出尾端和弦、`sparseCorrect()` 無法還原。
- 最小修正只在 `repeatEndingChordTail()` 的 1–3 字元前綴加入 `[`；Green 為 28/28。
- 正式無快取頁面載入完整兩頁原檔：第 2 頁第一列 `C` 保持，頁尾第一結尾 `Dm7 / G` 與第二結尾 `Dm7 / G7 / C` 全部有框，診斷日誌為 0。
- 已審核完整差異並只暫存原始碼、部署版、OCR 測試、專案知識與本計畫；既有 skill 刪除、其他未追蹤計畫／skill／`output`／`tmp` 均未納入。

## 驗證結果

- Red：加入 `[12.Dm7` 兩項案例後，原始版 26 項通過、2 項失敗。
- Green：原始碼與部署版 `tests/test_ocr.js` 各 28/28；`tests/test_mobile.js` 各 73/73。
- `python build_site.py` 成功；`git diff --check` 通過，正式碼沒有 OCR 診斷殘留。
- 完整原檔正式無快取端到端：第一列 `C`、第一結尾 `Dm7 / G`、第二結尾 `Dm7 / G7 / C` 全數有框。

## 剩餘事項

- 無。

## 模型接手指引

1. 先依 `task_id`／`thread_id` 辨識同一任務，再核對目前檔案與版本控制狀態。
2. 不把本紀錄視為唯一現況證據；重新執行與下一步相關的檢查。
3. 從第一個未勾選步驟接續，並在每個實質里程碑開始與完成時同步本檔。

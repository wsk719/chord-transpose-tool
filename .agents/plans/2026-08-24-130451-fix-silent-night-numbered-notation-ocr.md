---
task_id: "2f228a85-8b2c-42a5-b2e8-f4ebfac70b5d"
thread_id: ""
status: completed
created_at: "2026-08-24T13:04:51+08:00"
updated_at: "2026-08-24T13:28:29+08:00"
workspace_root: "D:\\AI\\譜"
title: "修正平安夜簡譜數字誤判和絃"
---

# 修正平安夜簡譜數字誤判和絃

## 成功條件

- [x] `平安夜.png` 三行輸出依序為 `C G C`、`F C F C`、`G G7 C C G7 C`，簡譜數字與歌詞上沒有假和絃框。
- [x] 新規則只在五線譜且有強簡譜數字列證據時啟用；一般和絃譜與既有特殊誤讀修正維持原行為。
- [x] 原始碼與部署版的 OCR 純函式回歸、建置及實際瀏覽器端到端驗證通過；互動測試因目前環境缺少 `jsdom` 無法執行，已以既有 19 和絃 PDF 實檔回歸補強。

## 範圍

- 修改唯一原始碼 `和弦轉調工具.html`、直接相關 OCR 測試、產生的 `docs/index.html`，必要時補充已驗證的專案知識。
- 使用 `D:\Download\平安夜.png` 作端到端回歸；優先在行級篩選與窄帶補掃邊界做最小修正。

## 不在範圍內

- 不更換 OCR 引擎、語言包或外部依賴。
- 不重寫既有通用和絃解析規則，不調整轉調與標註互動。

## 關鍵假設與風險

- 主要風險是把正常含數字後綴的和絃列（如 `G7 Cmaj7`）誤當簡譜列；啟用條件必須依大量純數字／延音符號，而不是只看少數數字。
- 工作樹起始已有不屬於本任務的 skill 刪除、其他計畫與 `output/`、`tmp/` 未追蹤內容；不得修改、暫存或提交它們。
- `tests/test_mobile.js` 基線因目前環境缺少 `jsdom` 無法執行；可使用工作區 bundled 依賴補足執行環境，但不自行安裝依賴。

## 執行計畫

- [x] 建立基線並確認成功條件。
- [x] 新增會重現簡譜列誤判的純函式測試並確認修改前失敗。
- [x] 實作僅對五線譜上方成對高密度簡譜列生效的窄帶補掃。
- [x] 產生部署版並執行純函式與瀏覽器端到端驗證。
- [x] 完成文件、記憶與 Git 收尾。

## 執行紀錄

### 2026-08-24T13:04:51+08:00

- 已建立持久化任務計畫。
- 基線：`node tests/test_ocr.js` 通過 32 項；`node tests/test_mobile.js` 因缺少 `jsdom` 而未啟動。上一輪已在部署版重現數字框與 `C67`／`F/A` 假陽性。

### 2026-08-24T13:07:02+08:00

- Red 證據：新增簡譜列分類、補掃裁切與框排除測試後，node tests/test_ocr.js 因正式碼尚無 isNumberedNotationLine 而失敗，確認測試會攔截未修正狀態。

### 2026-08-24T13:26:46+08:00

- 實際 Tesseract 證據推翻單行文字分類假設：簡譜被拆成相鄰兩列。改以五條譜線幾何位置加上譜線上方兩個高密度 OCR 列作窄觸發，並只用 PSM 6 掃描譜線上方 12 至 7 個譜線間距的和絃帶。
- 附件實測的三行原和絃為 `C G C`、`F C F C`、`G G7 C C G7 C`；升高一個半音後畫面精確顯示 `C# G# C#`、`F# C# F# C#`、`G# G#7 C# C# G#7 C#`，簡譜數字及歌詞皆無誤框。
- 另上傳《無價至寶》第 2 頁回歸 PDF，升高一個半音後既有 19 個和絃（含 slash chord、maj7、m7）皆保留，未見本次規則外溢。

## 驗證結果

- 修改前 OCR 純函式：32/32 通過。
- 修改後原始碼 OCR 純函式：46/46 通過。
- 修改後部署版 OCR 純函式：46/46 通過。
- `python build_site.py`：通過。
- 真實 `平安夜.png` 與既有 19 和絃 PDF：瀏覽器端到端通過。
- 手機互動測試：本機與 bundled workspace dependency 均無 `jsdom`，無法執行；本次未修改互動程式。

## 剩餘事項

- 無。

## 模型接手指引

1. 先依 `task_id`／`thread_id` 辨識同一任務，再核對目前檔案與版本控制狀態。
2. 不把本紀錄視為唯一現況證據；重新執行與下一步相關的檢查。
3. 從第一個未勾選步驟接續，並在每個實質里程碑開始與完成時同步本檔。

---
task_id: "8ac07326-d856-4341-875e-0e7358463e83"
thread_id: ""
status: completed
created_at: "2026-08-24T10:08:54+08:00"
updated_at: "2026-08-24T10:36:11+08:00"
workspace_root: "D:\\AI\\譜"
title: "修正第二頁反覆段落底部和弦漏抓"
---

# 修正第二頁反覆段落底部和弦漏抓

## 成功條件

- [x] 以使用者提供的原始 PDF 第二頁穩定重現底部反覆段落漏抓，定位第一個異常處而非只憑畫面猜測。
- [x] 第二頁底部反覆段落的 5 個和弦（Dm7、G、Dm7、G7、C）皆能被自動偵測，且不新增該區誤判。
- [x] 現有 OCR／手機測試、語法檢查與部署版建置全部通過；原始碼與部署版行為一致。

## 範圍

- 檢查 PDF 第二頁底部、OCR 前處理、稀疏救援與區段合併邏輯。
- 修改唯一原始碼 `和弦轉調工具.html`、相應回歸測試及產生的 `docs/index.html`。

## 不在範圍內

- 不修改原始 PDF 內容，不更換 OCR 引擎，不重構無關的轉調／標註／行動版介面。
- 不處理附件之外尚未重現的其他譜面辨識問題。

## 關鍵假設與風險

- 附圖中的藍框為目前工具實際辨識結果；仍須用現行程式和 PDF 重跑確認。
- 上方反覆結尾延長線可能讓字框與線條黏連、或讓寬度／行帶過濾排除正確候選；若放寬過度會把譜線、歌詞或小節號誤收為和弦。
- 工作樹原有刪除與未追蹤內容均視為使用者變更；本任務不修改或納入無關項目。

## 執行計畫

- [x] 建立基線並確認成功條件。
- [x] 完成最小連貫變更或分析。
- [x] 執行驗證並核對結果。
- [x] 完成文件、記憶與 Git 收尾。

## 執行紀錄

### 2026-08-24T10:08:54+08:00

- 已建立持久化任務計畫。
- 已讀取 PDF 與除錯／測試流程；記錄原始 PDF SHA-256 `0D16DC1B0FF85E35C3DBF456CFE9550C8EF6B0E921826E338DE92140C2E354D1`。
- Git 基線：`main` 比 `origin/main` 領先 5；存在使用者原有的 skill 刪除、未追蹤 `output/` 與 `tmp/`，不得混入本次提交。

### 2026-08-24T10:31:40+08:00

- 完整 PDF.js + Tesseract.js 重現：PSM 11 能建立頁尾窄帶，但 PSM 7 輸出為 `pm? / Gc / ||2-Dm7 / G7 / Cc`；舊 `scanChordBands()` 又送回一般 `detectFrom()`，未用 `sparseCorrect()`，因此漏掉 3 個目標和弦。
- 先驗證並撤回「擦除長延長線」方案：單變因測試顯示不擦線仍能抓全，故正式碼不改影像像素。
- 最小修正：只有補掃字詞含反覆結尾編號黏和弦標誌時，該帶才直接走稀疏解析；新增 `pm?→Dm7` 與編號前綴剝除，沿用 `Gc→G`，並保留單字母信心門檻 20。
- 視覺回歸：第二頁前兩列未新增歌詞／音符誤框；頁尾 `Dm7 / G / Dm7 / G7 / C` 全數有框，仍只有附件原本那 1 個音符誤框。

### 2026-08-24T10:33:46+08:00

- 最終證據閘門通過：部署版重建、原始碼／部署版各 14 項 OCR、各 64 項手機測試、兩份 inline script 語法解析與 `git diff --check` 全部成功；無暫時 OCR 診斷碼。
- 本次兩個測試目錄已從工作區移至系統暫存區（環境政策拒絕遞迴刪除）；本機 HTTP 測試伺服器與代理建立的瀏覽器頁籤已關閉。
- 已審核完整任務差異；本任務檔案沒有未暫存差異，使用者原有的兩個 skill 刪除、未追蹤 workflow skill、`output/` 與其他 `tmp/` 均保持不動。

### 2026-08-24T10:35:31+08:00

- 首次 `git commit` 因工作副本未設定作者身份而失敗，沒有建立提交；依流程暫時標為 `blocked`。
- 最近 5 個既有提交的作者一致為 `cowork <cowork@local>`，可用同一身份補上 repository-local 設定後重試，不需修改全域 Git 設定。

### 2026-08-24T10:35:56+08:00

- 已設定 repository-local 作者為既有的 `cowork <cowork@local>`；提交阻礙解除，未改動全域 Git 設定。

## 驗證結果

- `python build_site.py`：通過，部署版重建成功且無外部資源殘留錯誤。
- `node tests/test_ocr.js` 與部署版參數：各 14 項通過。
- `NODE_PATH=tmp/mobile-test-deps/node_modules node tests/test_mobile.js` 與部署版參數：各 64 項通過；`jsdom@26` 僅安裝於本次暫存測試目錄，驗證後已移出工作區。
- 本機瀏覽器以完整原始 PDF 與抽出的第二頁 PDF 驗證；正式頁面前兩列維持原框，頁尾 5/5 目標和弦皆辨識。

## 剩餘事項

- 無。

## 模型接手指引

1. 先依 `task_id`／`thread_id` 辨識同一任務，再核對目前檔案與版本控制狀態。
2. 不把本紀錄視為唯一現況證據；重新執行與下一步相關的檢查。
3. 從第一個未勾選步驟接續，並在每個實質里程碑開始與完成時同步本檔。

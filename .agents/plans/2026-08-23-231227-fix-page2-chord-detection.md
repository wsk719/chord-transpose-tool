---
task_id: "fix-page2-chord-detection"
thread_id: ""
status: completed
created_at: "2026-08-23T23:12:27+08:00"
updated_at: "2026-08-23T23:39:48+08:00"
workspace_root: "D:\\AI\\譜"
title: "修正 PDF 第二頁和弦辨識漏抓"
---

# 修正 PDF 第二頁和弦辨識漏抓

## 成功條件

- [x] 以使用者提供的第二頁結果重現目前只抓到 `E7 Am7 C/G C/E` 的問題。
- [x] 修正後可依閱讀順序抓出第二頁全部 19 個和弦：`G C G/B Am7 C/G Fmaj7 G G/F E7 E7/G# Am7 C/G F C/E Dm7 G Dm7 G7 C`。
- [x] 既有測試與新增的第二頁回歸測試全部通過，且實際介面／主要執行路徑仍可載入。

## 範圍

- 檢查 PDF／影像和弦辨識、候選篩選、排序及頁面處理程式。
- 加入第二頁漏抓案例的最小回歸測試並修正直接相關文件。

## 不在範圍內

- 不修改樂譜音符辨識、歌詞 OCR 或轉調規則，除非它們直接造成和弦漏抓。
- 不變更使用者原始 PDF，也不部署或發布網站。

## 關鍵假設與風險

- 圖中藍框是目前程式輸出的命中結果；無框和弦是漏抓目標。
- 第二頁含斜線和弦、升記號與一／二房括號，修正不得靠單一曲目硬編碼。
- 工作樹已有使用者變更與未追蹤輸出，修改與提交時必須隔離本任務檔案。

## 執行計畫

- [x] 建立基線並確認成功條件。
- [x] 完成最小連貫變更或分析。
- [x] 執行驗證並核對結果。
- [x] 完成文件、記憶與 Git 收尾。

## 執行紀錄

### 2026-08-23T23:12:27+08:00

- 已建立持久化任務計畫。
- 已確認原始頁面預期和弦共 19 個；目前結果僅命中 `E7 Am7 C/G C/E`。
- 已記錄基線工作樹含既有刪除與未追蹤內容，後續不覆寫或納入無關變更。

### 2026-08-23T23:17:23+08:00

- 初次近似基線誤把裁切 PNG 當成 PDF 路徑；後續三輪診斷確認高解析 PNG 原本可抓 19 個，真正失敗位於 PDF.js 先低解析渲染再放大的路徑。

### 2026-08-23T23:38:13+08:00

- 以實際兩頁 PDF 重現第二頁只命中 4 個目標和弦；PSM 11 已產生第一／三行多字符候選，但舊救援不允許建立新水平帶。
- PDF 初次渲染提高到目標 2600px／上限 5 倍；新增至少 3 個多字符錨點才啟動的 PSM 7 窄帶補掃、譜線避讓裁切、異常寬單字母拒絕及閱讀順序排序。
- 已新增 `tests/test_ocr.js`、重建 `docs/index.html`、更新 README 與專案知識；部署版實際畫面確認 6+8+5 共 19 個框且無額外框。

### 2026-08-23T23:39:32+08:00

- Git commit 因儲存庫未設定 user.name/user.email 而失敗；本任務檔案仍維持獨立 staged，未納入既有刪除或未追蹤輸出。解除條件：取得可安全沿用的作者身分後重試單次 commit。

### 2026-08-23T23:39:47+08:00

- 已確認 HEAD 既有作者為 cowork <cowork@local>；本次只透過單次 commit 環境變數沿用，不寫入儲存庫或全域 Git 設定。

## 驗證結果

- `build_site.py`：成功產生 `docs/index.html`，10 個 vendor 檔完整。
- 原始碼與部署版 `<script>`：`new Function` 語法檢查皆通過。
- `tests/test_ocr.js`：原始碼 9/9、部署版 9/9。
- `tests/test_mobile.js`：原始碼 64/64、部署版 64/64。
- 本機部署版＋第二頁 PDF：按閱讀順序得到 19 個目標和弦、零額外候選；三個系統皆目視確認藍框位置。
- `git diff --check`：通過；無空白錯誤。

## 剩餘事項

- 無。既有技能刪除、`output/`、`tmp/` 與未追蹤技能目錄不屬於本任務，已排除於提交之外。

## 模型接手指引

1. 先依 `task_id`／`thread_id` 辨識同一任務，再核對目前檔案與版本控制狀態。
2. 不把本紀錄視為唯一現況證據；重新執行與下一步相關的檢查。
3. 從第一個未勾選步驟接續，並在每個實質里程碑開始與完成時同步本檔。

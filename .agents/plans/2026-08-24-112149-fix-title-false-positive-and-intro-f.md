---
task_id: "03727579-4b87-4729-873a-83e21e6bd22d"
thread_id: ""
status: completed
created_at: "2026-08-24T11:21:49+08:00"
updated_at: "2026-08-24T11:37:24+08:00"
workspace_root: "D:\\AI\\譜"
title: "修正 PDF 前段標題誤判與前奏 F 漏判"
---

# 修正 PDF 前段標題誤判與前奏 F 漏判

## 成功條件

- [x] 以附件 PDF 第一頁穩定重現：標題「神」左上多一個假和弦框，前奏第三個單字母 `F` 漏框。
- [x] 修正後標題區不再產生該假候選，前奏第三個 `F` 會被辨識，且既有同頁和弦不退化。
- [x] 原始碼與部署版通過針對性回歸測試、完整相關測試、語法檢查與真實 PDF 端到端視覺核對。

## 範圍

- 檢查附件 PDF 第一頁、OCR 原始輸出、候選修正與篩選流程。
- 最小修改 `和弦轉調工具.html`、對應測試與由 `build_site.py` 產生的部署版。

## 不在範圍內

- 不修改原始 PDF 內容或版面。
- 不調整與本重現案例無關的 UI、轉調或匯出功能。

## 關鍵假設與風險

- 假設截圖為目前工作樹版本對附件 PDF 第一頁的辨識結果；先由真實路徑固定 OCR 證據後再修正。
- 單字母和弦救援若過度放寬，容易把中文／圖形誤判成 `A`–`G`，修正必須有版面或同行上下文約束。

## 執行計畫

- [x] 建立基線並確認成功條件。
- [x] 完成最小連貫變更或分析。
- [x] 執行驗證並核對結果。
- [x] 完成文件、記憶與 Git 收尾。

## 執行紀錄

### 2026-08-24T11:21:49+08:00

- 已建立持久化任務計畫。
- 已讀取 PDF 檢視、專案除錯流程與既有 OCR 專案知識；目前工作樹已有使用者／前序任務變更，後續只納入本任務可辨識差異。
- PDF 視覺基線：單頁 A4，目標區為頁首；截圖假框在標題「神」字上緣，漏框為 `Or:` 列第三個 `F`。已暫加可移除診斷資料，用於固定兩輪 OCR 原始字詞、信心與座標。

### 2026-08-24T11:34:33+08:00

- 真實 PDF 兩輪 OCR 證據：標題碎片為 `AY`／`A)`，第二輪 `A)` 信心 61.7 被舊解析器去掉右括號後誤收為 `A`；`Or:` 列目標 F 兩輪均為 `'F`，信心 9.6／0。
- `A)` 的最早異常在 `correctToken()` 無條件剝除不成對的右括號；`'F` 先因前導撇號無法解析而未進救援，加入受限解析後又因撇號拉高字框至一般候選的 1.68 倍、超過 1.6 倍字高門檻而被拒。
- 最小修正：拒絕可解析和弦後方不成對的 `)`，但保留 `(A)`；前導撇號候選不進一般解析，只送入既有「同行至少 3 個和弦」救援，並只為此類垂直前綴採 1.75 倍字高上限。
- 回歸測試先得到 21 通過／2 失敗，再完成純函式修正。完整 PDF 臨時診斷驗證頁首假 `A` 消失，`Or:` 列依序得到 `C / C / F / G`；診斷碼已移除。

### 2026-08-24T11:36:15+08:00

- 已重建 `docs/index.html`，更新專案知識並完成證據閘門；最終原始頁面再次載入同一 PDF，可完成 OCR、顯示工具列，且 DOM 無任何診斷節點。

### 2026-08-24T11:37:09+08:00

- 已審核完整任務差異並只暫存原始碼、部署版、OCR 測試、專案知識與本計畫；既有 skill 刪除及未追蹤的 workflow skill、`output/`、`tmp/` 均未納入。

## 驗證結果

- Red：`node tests/test_ocr.js` 得 21 通過／2 失敗，分別固定 `A)` 假候選與 `'F` 未能進救援。
- Green：原始碼與部署版 `tests/test_ocr.js` 各 24/24；原始碼與部署版 `tests/test_mobile.js` 各 73/73。
- `python build_site.py` 成功；原始碼 1 段、部署版 2 段 inline script 均可由 `new Function` 解析；`git diff --check` 通過。
- 真實 PDF.js + Tesseract.js：修正前頁首 15 個候選含標題假 `A`、漏 `Or:` 的 `F`；修正後標題 `A` 消失，`Or:` 行新增正確 `F`，其餘頁首候選座標與文字維持一致。

## 剩餘事項

- 無。

## 模型接手指引

1. 先依 `task_id`／`thread_id` 辨識同一任務，再核對目前檔案與版本控制狀態。
2. 不把本紀錄視為唯一現況證據；重新執行與下一步相關的檢查。
3. 從第一個未勾選步驟接續，並在每個實質里程碑開始與完成時同步本檔。

---
task_id: "96f3ba96-b5cc-474a-85bd-f2ca3e1e9f37"
thread_id: ""
status: completed
created_at: "2026-08-24T11:48:01+08:00"
updated_at: "2026-08-24T12:04:11+08:00"
workspace_root: "D:\\AI\\譜"
title: "修正譜線直線黏連造成 C 和弦漏辨識"
---

# 修正譜線直線黏連造成 C 和弦漏辨識

## 成功條件

- [x] 附件 PDF 第 2 頁第一列的第二個 `C`（下方黏直線）可被自動辨識並顯示標註框。
- [x] 同頁既有正確和弦仍保持正確，且不因放寬規則增加明顯假陽性。
- [x] 原始版與部署版通過針對性 OCR 回歸測試、語法檢查與建置驗證。

## 範圍

- 檢查附件第 2 頁在 PDF.js、影像前處理、Tesseract 與候選過濾各階段的實際資料。
- 最小修改 OCR 誤讀修正／同行救援與相關測試；重建 `docs/index.html`。

## 不在範圍內

- 不修改附件 PDF 內容，不重做整套 OCR，也不調整無關的 UI／轉調功能。

## 關鍵假設與風險

- 使用者所指的是截圖中第一列、位於 `G` 之後且接近雙小節線的 `C`。
- 單字母和弦很容易與歌詞／譜面碎片混淆；任何新修正都必須限定於可信的和弦列或形態，避免全域放寬。
- 工作樹已有使用者／其他任務的刪除與未追蹤產物，本任務不碰觸或納入那些變更。

## 執行計畫

- [x] 建立基線並確認成功條件。
- [x] 完成最小連貫變更或分析。
- [x] 執行驗證並核對結果。
- [x] 完成文件、記憶與 Git 收尾。

## 執行紀錄

### 2026-08-24T11:48:01+08:00

- 已建立持久化任務計畫。
- 已檢視使用者截圖；漏框目標位於第一列第二個和弦 `C`，字母下方緊鄰一條垂直線。
- 已確認現有程式先前已處理同份 PDF 的反覆結尾橫線案例，但尚未證明本次垂直線黏連案例已修復。
- 初次用裁切單頁測試檔時目標 `C` 可辨識，但 pypdf 證明其頁框／內容流不等同原附件；改以完整原檔為唯一端到端基線，確認原檔第 2 頁仍漏掉目標，撤回「現行版本已修復」的早期判斷。
- 完整原檔的 PSM 3 灰階／原色兩輪沒有輸出第一列單字母；PSM 11 用多字元和弦建立窄帶後，舊 `cy + 0.9h` 下緣吃到 `C` 下方直線，PSM 7 把 x=670–1016 合成信心 0 的 `Sc`。
- 單變因把下緣收至 `cy + 0.5h` 後，同一原檔輸出獨立 `C`（信心 86.53、x=988–1016），第一列畫面恢復 `G / C / G/B / Am7 / C/G / Fmaj7`，第二、三列既有框保持。
- 已新增 `sparseBandCrop()` 並補兩項純函式回歸（目標裁切值與影像邊界）；所有暫時診斷碼已移除。以新網址避開瀏覽器快取後重跑完整兩頁原檔，正式頁面診斷日誌數為 0，目標 `C` 有正確藍框。
- 已審核完整差異並只暫存原始碼、部署版、OCR 測試、專案知識與本計畫；既有兩個 skill 刪除、其他未追蹤計畫／skill／`output`／`tmp` 均未納入。

## 驗證結果

- Red（完整原檔）：舊下緣 `cy + 0.9h` 的 PSM 7 輸出 `Sc`、信心 0，畫面漏框。
- Green（完整原檔）：新下緣 `cy + 0.5h` 的 PSM 7 輸出 `C`、信心 86.53；正式無快取頁面第 2 頁目標有框，且無任何 `ocr-debug` 日誌。
- `node tests/test_ocr.js` 與部署版參數：各 26 項通過、0 失敗。
- `node tests/test_mobile.js` 與部署版參數：各 73 項通過、0 失敗。
- `python build_site.py`：成功重建 81 KB `docs/index.html` 與 10 個 vendor 檔；`git diff --check` 通過，正式碼無診斷殘留。

## 剩餘事項

- 無。

## 模型接手指引

1. 先依 `task_id`／`thread_id` 辨識同一任務，再核對目前檔案與版本控制狀態。
2. 不把本紀錄視為唯一現況證據；重新執行與下一步相關的檢查。
3. 從第一個未勾選步驟接續，並在每個實質里程碑開始與完成時同步本檔。
